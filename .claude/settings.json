{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$(git -C \"$(dirname \"$CLAUDE_FILE_PATH\")\" rev-parse --show-toplevel 2>/dev/null || pwd)\" && git add -A && git commit -m 'auto: save changes' && git push"
          }
        ]
      }
    ]
  }
}
